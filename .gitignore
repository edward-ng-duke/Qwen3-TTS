__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.idea/
.vscode/
venv/
env/
.env

# Pre-downloaded model weights (multi-GB; baked into the Docker image at build time).
models/
followups.md

# Local debug / decode artifacts (e.g. decoded_single_12hz.wav)
*.wav
# ...but the bundled clone-reference clips ARE required UI assets (served to the
# Base/Clone studio's "示例参考音色"), so keep them version-controlled.
!web/public/samples/clone-ref/*.wav

# Local tooling / agent state (not project source).
.claude/
.playwright-mcp/
.cache/
/auth_doc/jwt

# Ad-hoc UI iteration screenshots at repo root.
/empty.png
/player-after*.png

# Agent-generated UI/UX review reports (multi-MB HTML; not project source).
/report/

# Deploy bundle: compressed image tarball + copied .env/jwt for cross-machine deploy.
/deploy-bundle/
